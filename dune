(data_only_dirs output)

(executables
  (names sim_single sim_many)
  (package hotpow)
  (public_names powsim many)
  (libraries cmdliner stdio)
  (preprocess
    (pps ppx_deriving_cmdliner ppx_hash ppx_deriving.show)))
